# Dragon Clan Monetizer — Environment Variables
# Copy this to .env and fill in values

# === REQUIRED ===

# Azure Cosmos DB connection string
COSMOS_CONNECTION=AccountEndpoint=https://YOUR_ACCOUNT.documents.azure.com:443/;AccountKey=YOUR_KEY;

# === Azure OpenAI (required for AI features) ===
AZURE_OPENAI_KEY=your-azure-openai-key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-41

# === OPTIONAL ===

# Server port (default: 5000)
PORT=5000

# JWT secret for auth (shared with DCTV/Studio for SSO)
JWT_SECRET=your-jwt-secret

# Redis de-dupe cache (for cross-system email deduplication)
REDIS_HOST=your-redis.redis.cache.windows.net
REDIS_PORT=6380
REDIS_KEY=your-redis-access-key
REDIS_SSL=true
